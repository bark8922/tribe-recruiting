# Workspace Backup

Off-machine backup of the local `Recruiting Dashboard` working folder (docs, scope files,
scripts, SQL, small CSVs). Excludes regenerable/heavy material: node_modules, build output,
the large data dumps (dashboard_data*.json), and legacy binary archives (powerbi_export,
legacy-pbix) which live at the repo root.

PROJECT_LOG.md here is the single source of truth for project status and decisions.
Re-synced by Claude and by a daily scheduled push. Last manual sync: 2026-06-19.
